# config.yml
local_m3u_path: "playlist_local.m3u"

server:
  host: "0.0.0.0"
  port: 3037
# Optional: override hardware detection by explicitly specifying the profile here
# Leave empty to auto-detect hardware
# Options: hevc_nvenc, h264_nvenc, hevc_amf, h264_amf, hevc_vaapi,
#          h264_vaapi, hevc_qsv, h264_qsv, software_libx265, software_libx264
ffmpeg_profile: "hevc_nvenc"

